import "./Layout.scss";

export const Layout66 = ({ children, className = "" }) => {
  return (
    <section className={`layout layout-66 ${className}`}>{children}</section>
  );
};

export const Layout84 = ({ children, className = "" }) => {
  return (
    <section className={`layout layout-84 ${className}`}>{children}</section>
  );
};

export const Layout48 = ({ children, className = "" }) => {
  return (
    <section className={`layout layout-48 ${className}`}>{children}</section>
  );
};

export const Layout39 = ({ children, className = "" }) => {
  return (
    <section className={`layout layout-39 ${className}`}>{children}</section>
  );
};

export const Layout93 = ({ children, className = "" }) => {
  return (
    <section className={`layout layout-93 ${className}`}>{children}</section>
  );
};

export const Layout1212 = ({ children, className = "" }) => {
  return (
    <section className={`layout layout-1212 ${className}`}>{children}</section>
  );
};
